{
  "name": "radio-app",
  "version": "1.2.0",
  "description": "Интырнэт Радиво - приложение для прослушивания радио",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "clean": "rm -rf node_modules dist src-tauri/target .vite .tauri && find . -name '*.log' -type f -delete 2>/dev/null || true"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^5.0.0"
  }
}
